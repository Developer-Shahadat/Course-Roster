
import PropTypes from 'prop-types';

const Cart = () => {
    
    return (
       <div>
             
       </div>
    );
};

Cart.propTypes = {
    cartId: PropTypes.array.isRequired
};

export default Cart;


